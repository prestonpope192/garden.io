# Raw Screenshot References

This directory is reserved for raw Stitch screenshots or source-linked screenshot reference files.

Current implementation notes:
- `manifest.json` tracks every accessible Stitch screen and its classification.
- route implementation should prioritize importing screenshots for the `implementationTarget` screens first when visual diffs are needed.
- uploaded assets or non-UI screenshots remain reference-only.
