# Raw HTML References

This directory is reserved for raw Stitch HTML exports or source-linked HTML reference files.

Current implementation notes:
- `manifest.json` is the canonical inventory for every accessible Stitch screen.
- implementation work should use the manifest's `rawBaseName` values when adding direct exports here.
- uploaded markdown/spec screens remain reference-only even when their HTML is imported.
